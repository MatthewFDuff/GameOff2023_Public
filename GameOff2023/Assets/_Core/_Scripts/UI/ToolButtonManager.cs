using UnityEngine;

namespace _Core._Scripts.UI
{
    /// <summary>
    /// Manages the tool buttons in the UI. Automatically creates them so we don't have to.
    /// </summary>
    public class ToolButtonManager : MonoBehaviour
    {
        [SerializeField] private GameObject _toolButtonPrefab;
        
        
    }
}
